> lstnew qui garde une reference plutot que dup
> strjoin qui free s1
> fonction que free un char **
> memdup
> strdup qui s'arrete a un certain caractere
> strlen qui s'arrete a un certain caractere
> strdup qui s'arrete au bout de x caracteres
> ft_split_whitespaces (split une string a tout les whitespaces)

> fonction qui print un message ainsi que son numero d’occurence pour quand tu place du debug tous au long de ton code tu sache qu’elle est l’erreur qui a peter :simple_smile:
​> fonction qui prend un string en paramettre pour afficher un message d’erreur (c’elle la tu en auras besoin sur tout tes projet de premiere année)




BZERO
STRTRIM



